<Project>

  <!-- The build kit's guards, manifest writer and signing step. Pairs with the props import in
       Directory.Build.props; importing one without the other fails with ZZB001. -->

  <Import Project="ZeroZero.Build.targets" Sdk="ZeroZero.Build" />

</Project>
